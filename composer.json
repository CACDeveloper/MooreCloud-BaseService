{
    "require": {
        "predis/predis": "^1.1",
        "firebase/php-jwt": "^5.0",
        "zendframework/zend-config": "^3.2",
        "zendframework/zend-http": "~2.3"
    }
}
$client = new Predis\Client();
                $client->set($username, 'lalsdaldsjsad');
                $return = $client->get($username);